# Procedural batch (100 rows).

seed: 103
num_documents: 100
engine: procedural
document_type: transcript
document_profiles_dir: ../../ner-gold-generator/configs/document_profiles

entities_per_document:
  distribution: fixed
  fixed: 5
